"""Connector handlers written by the Connector Agent.

Each module here is one connector's Python handlers, generated from a work order
and delivered as a pull request rather than written into a running site — see
``connectors/handler_authoring.py``. Files are ordinary application code once
merged; edit them like anything else.
"""
