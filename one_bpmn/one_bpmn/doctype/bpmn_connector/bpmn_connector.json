{
 "actions": [],
 "allow_rename": 0,
 "autoname": "field:connector_id",
 "creation": "2026-07-29 08:00:00",
 "description": "A configurable outbound connector for BPMN Service Tasks (serviceType=\"connector\"). Declares the provider, its canvas icon, how it authenticates and how it executes. Operations (and their fields) live in BPMN Connector Operation.",
 "doctype": "DocType",
 "engine": "InnoDB",
 "field_order": [
  "connector_id",
  "label",
  "enabled",
  "column_break_basics",
  "description",
  "icon_section",
  "icon_svg_path",
  "icon_color",
  "column_break_icon",
  "icon_label",
  "icon_preview",
  "execution_section",
  "execution_type",
  "base_url",
  "column_break_exec",
  "request_timeout",
  "allow_internal_hosts",
  "auth_section",
  "auth_type",
  "credential_source",
  "auth_secret",
  "auth_settings_doctype",
  "auth_secret_field",
  "column_break_auth",
  "auth_header_name",
  "auth_query_param",
  "api_section",
  "api_name",
  "api_version",
  "column_break_api",
  "discovery_url"
 ],
 "fields": [
  {
   "description": "Stable id used in the BPMN XML (spiffworkflow:connectorId). Lowercase with underscores, e.g. <code>google_drive</code>.",
   "fieldname": "connector_id",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "Connector ID",
   "reqd": 1,
   "unique": 1
  },
  {
   "description": "Shown in the Connector dropdown in the modeler.",
   "fieldname": "label",
   "fieldtype": "Data",
   "in_list_view": 1,
   "label": "Label",
   "reqd": 1
  },
  {
   "default": "1",
   "description": "Disabled connectors disappear from the modeler and refuse to dispatch.",
   "fieldname": "enabled",
   "fieldtype": "Check",
   "in_list_view": 1,
   "label": "Enabled"
  },
  {
   "fieldname": "column_break_basics",
   "fieldtype": "Column Break"
  },
  {
   "fieldname": "description",
   "fieldtype": "Small Text",
   "label": "Description"
  },
  {
   "fieldname": "icon_section",
   "fieldtype": "Section Break",
   "label": "Canvas Icon"
  },
  {
   "description": "The <code>d</code> attribute of a single SVG path on a <b>24\u00d724 viewBox</b> (e.g. paste from an MDI icon). Drawn on the Service Task in place of the gear. Leave blank for the default plug.",
   "fieldname": "icon_svg_path",
   "fieldtype": "Small Text",
   "label": "Icon SVG Path"
  },
  {
   "default": "#14b8a6",
   "description": "Hex colour for the icon, e.g. <code>#14b8a6</code>.",
   "fieldname": "icon_color",
   "fieldtype": "Data",
   "label": "Icon Colour"
  },
  {
   "fieldname": "column_break_icon",
   "fieldtype": "Column Break"
  },
  {
   "description": "Tooltip/label for the icon (defaults to Label).",
   "fieldname": "icon_label",
   "fieldtype": "Data",
   "label": "Icon Label"
  },
  {
   "fieldname": "icon_preview",
   "fieldtype": "HTML",
   "label": "Preview"
  },
  {
   "fieldname": "execution_section",
   "fieldtype": "Section Break",
   "label": "Execution"
  },
  {
   "default": "HTTP Request",
   "description": "<b>HTTP Request</b> \u2014 fully configured here, no Python needed. <b>Python Handler</b> \u2014 dispatch to a registered @connector handler (or an operation's Handler Path); use for SDK-based providers.",
   "fieldname": "execution_type",
   "fieldtype": "Select",
   "in_list_view": 1,
   "label": "Execution Type",
   "options": "HTTP Request\nPython Handler",
   "reqd": 1
  },
  {
   "depends_on": "eval:doc.execution_type=='HTTP Request'",
   "description": "Prefixed to each operation's relative URL, e.g. <code>https://api.example.com/v2</code>.",
   "fieldname": "base_url",
   "fieldtype": "Data",
   "label": "Base URL"
  },
  {
   "fieldname": "column_break_exec",
   "fieldtype": "Column Break"
  },
  {
   "default": "30",
   "depends_on": "eval:doc.execution_type=='HTTP Request'",
   "description": "Seconds before an HTTP call is abandoned.",
   "fieldname": "request_timeout",
   "fieldtype": "Int",
   "label": "Request Timeout (s)"
  },
  {
   "default": "0",
   "depends_on": "eval:doc.execution_type=='HTTP Request'",
   "description": "Off (recommended): loopback, link-local and private-range hosts are refused, so a connector cannot be pointed at internal infrastructure. Turn on only for a deliberate internal integration.",
   "fieldname": "allow_internal_hosts",
   "fieldtype": "Check",
   "label": "Allow Internal Hosts"
  },
  {
   "depends_on": "eval:doc.execution_type=='HTTP Request'",
   "fieldname": "auth_section",
   "fieldtype": "Section Break",
   "label": "Authentication"
  },
  {
   "default": "None",
   "description": "How the secret is presented. <b>Service Account JSON</b> is handled by the Python handlers (Google) rather than the HTTP executor.",
   "fieldname": "auth_type",
   "fieldtype": "Select",
   "label": "Auth Type",
   "options": "None\nBearer Token\nAPI Key Header\nAPI Key Query Param\nBasic\nService Account JSON"
  },
  {
   "default": "On this connector",
   "depends_on": "eval:doc.auth_type && doc.auth_type!='None' && doc.auth_type!='Service Account JSON'",
   "description": "<b>On this connector</b> \u2014 type the secret below; it is encrypted and never leaves the server. <b>From a settings DocType</b> \u2014 read it from a Password field elsewhere, for a credential several connectors share (how the Google connectors reuse one service account).",
   "fieldname": "credential_source",
   "fieldtype": "Select",
   "label": "Credential Source",
   "options": "On this connector\nFrom a settings DocType"
  },
  {
   "depends_on": "eval:doc.credential_source!='From a settings DocType' && doc.auth_type && doc.auth_type!='None' && doc.auth_type!='Service Account JSON'",
   "description": "Stored encrypted, excluded from exports, and never sent to the browser.",
   "fieldname": "auth_secret",
   "fieldtype": "Password",
   "label": "Secret"
  },
  {
   "depends_on": "eval:doc.credential_source=='From a settings DocType'",
   "description": "Single DocType holding the shared secret, e.g. <code>Processa Settings</code>. Read with get_password at call time.",
   "fieldname": "auth_settings_doctype",
   "fieldtype": "Link",
   "label": "Secret Settings DocType",
   "options": "DocType"
  },
  {
   "depends_on": "eval:doc.credential_source=='From a settings DocType'",
   "description": "Password fieldname on that settings DocType, e.g. <code>google_drive_service_account_json</code>.",
   "fieldname": "auth_secret_field",
   "fieldtype": "Data",
   "label": "Secret Fieldname"
  },
  {
   "fieldname": "column_break_auth",
   "fieldtype": "Column Break"
  },
  {
   "default": "X-API-Key",
   "depends_on": "eval:doc.auth_type=='API Key Header'",
   "fieldname": "auth_header_name",
   "fieldtype": "Data",
   "label": "API Key Header Name"
  },
  {
   "depends_on": "eval:doc.auth_type=='API Key Query Param'",
   "description": "Query-string parameter carrying the key, e.g. <code>api_key</code>.",
   "fieldname": "auth_query_param",
   "fieldtype": "Data",
   "label": "API Key Query Param"
  },
  {
   "collapsible": 1,
   "fieldname": "api_section",
   "fieldtype": "Section Break",
   "label": "Provider API (documentation)"
  },
  {
   "fieldname": "api_name",
   "fieldtype": "Data",
   "label": "API Name"
  },
  {
   "fieldname": "api_version",
   "fieldtype": "Data",
   "label": "API Version"
  },
  {
   "fieldname": "column_break_api",
   "fieldtype": "Column Break"
  },
  {
   "description": "Reference only \u2014 never fetched at runtime.",
   "fieldname": "discovery_url",
   "fieldtype": "Data",
   "label": "Discovery Document URL"
  }
 ],
 "index_web_pages_for_search": 1,
 "links": [
  {
   "group": "Configuration",
   "link_doctype": "BPMN Connector Operation",
   "link_fieldname": "connector"
  }
 ],
 "modified": "2026-07-29 08:00:00",
 "modified_by": "Administrator",
 "module": "ONE BPMN",
 "name": "BPMN Connector",
 "naming_rule": "By fieldname",
 "owner": "Administrator",
 "permissions": [
  {
   "create": 1,
   "delete": 1,
   "email": 1,
   "export": 1,
   "print": 1,
   "read": 1,
   "report": 1,
   "role": "System Manager",
   "share": 1,
   "write": 1
  }
 ],
 "row_format": "Dynamic",
 "sort_field": "modified",
 "sort_order": "DESC",
 "states": [],
 "track_changes": 1
}
