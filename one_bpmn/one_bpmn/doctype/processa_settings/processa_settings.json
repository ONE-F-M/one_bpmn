{
 "actions": [],
 "creation": "2025-01-01 00:00:00",
 "doctype": "DocType",
 "engine": "InnoDB",
 "field_order": [
  "section_break_instance",
  "instance_type",
  "section_break_1",
  "connect_to_production",
  "production_url",
  "production_api_key",
  "production_api_secret",
  "section_break_ba",
  "enable_ba_sync",
  "ba_site_url",
  "ba_api_key",
  "ba_api_secret",
  "section_break_github",
  "github_token",
  "customization_app",
  "connector_handler_app",
  "section_break_script_security",
  "script_security_intro",
  "script_binop_multiply_threshold",
  "column_break_script_security",
  "script_block_while",
  "script_block_functiondef",
  "script_block_lambda",
  "script_strict_builtins",
  "script_strict_frappe_attrs",
  "script_block_all_imports",
  "section_break_ai_rate_limit",
  "column_break_ai_rate_limit",
  "lock_release_roles",
  "section_break_ai_memory",
  "default_memory_distill_model",
  "column_break_ai_memory",
  "default_memory_reconcile_model",
  "default_compaction_model",
  "default_context_token_budget",
  "llm_defaults_tab",
  "default_llm_provider",
  "openai_search_context",
  "observability_tab",
  "section_break_observability",
  "enable_observability",
  "langsmith_api_key",
  "langsmith_project_name",
  "observability_sanitization_level",
  "observability_retry_attempts",
  "section_break_langfuse",
  "langfuse_base_url",
  "langfuse_public_key",
  "column_break_langfuse",
  "langfuse_secret_key",
  "dev_agent_weekly_report_enabled",
  "dev_agent_report_recipients",
  "section_break_token_estimator",
  "token_estimator_chars_per_token",
  "mcp_integration_tab",
  "enabled",
  "mcp_tools_enabled",
  "enable_mcp_tools_for_user_planning_agent",
  "user_story_template",
  "jira_credentials_section",
  "jira_url",
  "jira_email",
  "jira_api_token",
  "copilot_settings_section",
  "copilot_metrics_pat",
  "copilot_org_name",
  "copilot_enterprise_slug"
 ],
 "fields": [
  {
   "fieldname": "section_break_instance",
   "fieldtype": "Section Break",
   "label": "Instance Configuration"
  },
  {
   "default": "Local",
   "description": "Identifies the role of this site so Processa can behave appropriately per environment. \"Review Doctypes/Workflow\" is available only on a BA instance; \"Reassign User Task\" only on a Production instance.",
   "fieldname": "instance_type",
   "fieldtype": "Select",
   "label": "Instance Type",
   "options": "Local\nProduction\nStaging\nBA"
  },
  {
   "fieldname": "section_break_1",
   "fieldtype": "Section Break",
   "label": "Production Site Configuration"
  },
  {
   "depends_on": "eval:doc.connect_to_production==1",
   "description": "Ex: https://one-fm.com",
   "fieldname": "production_url",
   "fieldtype": "Data",
   "label": "Production URL",
   "mandatory_depends_on": "eval:doc.connect_to_production==1"
  },
  {
   "depends_on": "eval:doc.connect_to_production==1",
   "fieldname": "production_api_key",
   "fieldtype": "Password",
   "label": "Production API Key",
   "mandatory_depends_on": "eval:doc.connect_to_production==1"
  },
  {
   "depends_on": "eval:doc.connect_to_production==1",
   "fieldname": "production_api_secret",
   "fieldtype": "Password",
   "label": "Production API Secret",
   "mandatory_depends_on": "eval:doc.connect_to_production==1"
  },
  {
   "default": "1",
   "description": " If checked, a Process Implementation with \"Active\" status in Production is required to make changes to a process map.",
   "fieldname": "connect_to_production",
   "fieldtype": "Check",
   "label": "Connect to Production"
  },
  {
   "fieldname": "section_break_ba",
   "fieldtype": "Section Break",
   "label": "BA Site Configuration"
  },
  {
   "default": "0",
   "fieldname": "enable_ba_sync",
   "fieldtype": "Check",
   "label": "Enable BA Sync"
  },
  {
   "depends_on": "eval:doc.enable_ba_sync==1",
   "description": "Ex: https://ba.one-fm.com",
   "fieldname": "ba_site_url",
   "fieldtype": "Data",
   "label": "BA Site URL",
   "mandatory_depends_on": "eval:doc.enable_ba_sync==1"
  },
  {
   "depends_on": "eval:doc.enable_ba_sync==1",
   "fieldname": "ba_api_key",
   "fieldtype": "Password",
   "label": "BA API Key",
   "mandatory_depends_on": "eval:doc.enable_ba_sync==1"
  },
  {
   "depends_on": "eval:doc.enable_ba_sync==1",
   "fieldname": "ba_api_secret",
   "fieldtype": "Password",
   "label": "BA API Secret",
   "mandatory_depends_on": "eval:doc.enable_ba_sync==1"
  },
  {
   "collapsible": 1,
   "depends_on": "eval:doc.connect_to_production==1",
   "fieldname": "section_break_github",
   "fieldtype": "Section Break",
   "label": "GitHub Integration"
  },
  {
   "description": "Personal (or fine-grained) access token able to push branches and open pull requests. Used by \"Review Doctypes\" \u2192 \"Sync\" to raise a PR with the DocType schema changes. The PR targets the repository of the app named in \"Customization Owner App\" below (or the DocType's own app when that app owns it), on that repository's default branch.",
   "fieldname": "github_token",
   "fieldtype": "Password",
   "label": "GitHub Access Token"
  },
  {
   "default": "one_fm",
   "description": "App whose source carries customizations made to <i>other</i> apps' DocTypes. A Custom Field on Employee belongs to this app, not to erpnext \u2014 so \"Sync\" writes it here, following this app's own convention (a data module under <code>custom/custom_field/</code> plus <code>custom/property_setter/</code>, registered in <code>setup/</code>, applied by a patch). A DocType owned by an app that is itself a ONE-F-M repo is written to that app instead. Leave blank to fall back to the DocType's owning app.",
   "fieldname": "customization_app",
   "fieldtype": "Data",
   "label": "Customization Owner App"
  },
  {
   "default": "one_bpmn",
   "description": "App whose repository receives the pull request when the Connector Agent writes a Python handler for a connector operation. Most connectors need no code at all \u2014 an operation is normally a declarative HTTP request \u2014 but a signed request, walked pagination, or a call sequence needs a real function, and that function is delivered as a pull request rather than written into this site. Handlers land in <code>connectors/generated/</code> in the named app, one module per connector, and the operation's Handler Path points at them. Defaults to the app that owns the connector layer. Leave blank to disable handler authoring entirely.",
   "fieldname": "connector_handler_app",
   "fieldtype": "Data",
   "label": "Connector Handler App"
  },
  {
   "collapsible": 1,
   "fieldname": "section_break_script_security",
   "fieldtype": "Section Break",
   "label": "Script Task Security"
  },
  {
   "fieldname": "script_security_intro",
   "fieldtype": "HTML",
   "options": "<p style=\"color:var(--text-muted);\">Controls the pre-deployment security gate for BPMN Script Tasks. Dangerous escape vectors (exec/eval/getattr, frame-walking dunders, permission-bypass kwargs, memory bombs, destructive SQL) are <b>always blocked</b> and are not configurable here. The options below tighten the gate for constructs that also have legitimate uses; all default to <b>allowed</b>.</p>"
  },
  {
   "default": "50000",
   "description": "Reject multiplication whose factor exceeds this value (memory-bomb guard, e.g. \"A\" * 999999999). Leave at 50000 unless you have a reason to change it.",
   "fieldname": "script_binop_multiply_threshold",
   "fieldtype": "Int",
   "label": "Multiplication Factor Threshold",
   "non_negative": 1
  },
  {
   "fieldname": "column_break_script_security",
   "fieldtype": "Column Break"
  },
  {
   "default": "0",
   "description": "Block <code>while</code> loops in script tasks (use a BPMN gateway/loop instead).",
   "fieldname": "script_block_while",
   "fieldtype": "Check",
   "label": "Block while Loops"
  },
  {
   "default": "0",
   "description": "Block function definitions (<code>def</code> / <code>async def</code>) in script tasks.",
   "fieldname": "script_block_functiondef",
   "fieldtype": "Check",
   "label": "Block Function Definitions"
  },
  {
   "default": "0",
   "description": "Block <code>lambda</code> expressions in script tasks.",
   "fieldname": "script_block_lambda",
   "fieldtype": "Check",
   "label": "Block lambda Expressions"
  },
  {
   "default": "0",
   "description": "Also block the commonly-legitimate builtins <code>hasattr / type / id / classmethod / staticmethod</code>.",
   "fieldname": "script_strict_builtins",
   "fieldtype": "Check",
   "label": "Strict Builtins"
  },
  {
   "default": "0",
   "description": "Also block the sensitive Frappe attributes <code>sql / commit / flags / conf / cache</code> (destructive raw SQL is always blocked regardless).",
   "fieldname": "script_strict_frappe_attrs",
   "fieldtype": "Check",
   "label": "Strict Frappe Attributes"
  },
  {
   "default": "0",
   "description": "Block ALL imports, not just the dangerous-module denylist.",
   "fieldname": "script_block_all_imports",
   "fieldtype": "Check",
   "label": "Block All Imports"
  },
  {
   "collapsible": 1,
   "description": "Who may release a frozen conversation. Site-wide because it is a statement about roles here, not about any one agent. The thresholds that DECIDE a freeze, and the message throttle, live on each AI Agent Configuration.",
   "fieldname": "section_break_ai_rate_limit",
   "fieldtype": "Section Break",
   "label": "AI Conversation Freeze"
  },
  {
   "fieldname": "column_break_ai_rate_limit",
   "fieldtype": "Column Break"
  },
  {
   "default": "AI Security Reviewer",
   "description": "Comma-separated roles allowed to release a frozen conversation. System Manager is always allowed. The locked user can never release their own conversation, whatever roles they hold.",
   "fieldname": "lock_release_roles",
   "fieldtype": "Small Text"
  },
  {
   "collapsible": 1,
   "description": "Site-wide defaults for the models that write long-term memory and compact conversations. An agent's own Memory Models override these; with all blank, memory writes use the agent's own AI Model, exactly as before.",
   "fieldname": "section_break_ai_memory",
   "fieldtype": "Section Break",
   "label": "AI Memory"
  },
  {
   "description": "Used when an agent has no Distillation Model of its own.",
   "fieldname": "default_memory_distill_model",
   "fieldtype": "Link",
   "label": "Default Distillation Model",
   "options": "AI Model"
  },
  {
   "fieldname": "column_break_ai_memory",
   "fieldtype": "Column Break"
  },
  {
   "description": "Used when an agent has no Reconciliation Model of its own. Tune here to fix poor reconciliations across every agent at once.",
   "fieldname": "default_memory_reconcile_model",
   "fieldtype": "Link",
   "label": "Default Reconciliation Model",
   "options": "AI Model"
  },
  {
   "description": "Used when nothing else names a model for summarising the early part of a conversation. Summarising is high-volume and low-stakes, so this should be the cheap model \u2014 left blank, compaction uses whatever the caller passes and, failing that, does nothing rather than bill the agent's own reasoning model for it.",
   "fieldname": "default_compaction_model",
   "fieldtype": "Link",
   "label": "Default Compaction Model",
   "options": "AI Model"
  },
  {
   "description": "Used when an agent sets no Context Token Budget of its own. Caps the assembled conversation history by estimated size, so a tool-heavy thread cannot outgrow the model's context window between compactions. 0 means no size limit.",
   "fieldname": "default_context_token_budget",
   "fieldtype": "Int",
   "label": "Default Context Token Budget"
  },
  {
   "description": "Migrated from onefm_mcp's AI Chat Settings doctype (retired). Global fallback provider/tuning for an agent configuration that has no linked AI Provider yet — once every agent config links a provider directly, this stops being consulted.",
   "fieldname": "llm_defaults_tab",
   "fieldtype": "Tab Break",
   "label": "LLM Defaults"
  },
  {
   "default": "anthropic",
   "description": "Global fallback provider for an agent configuration without a linked AI Provider record. Read by one_bpmn/agents/llm_provider/factory.py as the last resort before the hard-coded \"gemini\" default.",
   "fieldname": "default_llm_provider",
   "fieldtype": "Select",
   "label": "Default LLM Provider",
   "options": "gemini\nanthropic\nopenai"
  },
  {
   "default": "medium",
   "depends_on": "eval:doc.default_llm_provider=='openai'",
   "description": "Has a real configured value in production — restore this field's consumer (believed to be a BPMN Script Task node's inline script) to read it from here.",
   "fieldname": "openai_search_context",
   "fieldtype": "Select",
   "label": "OpenAI Search Context",
   "options": "low\nmedium\nhigh"
  },
  {
   "description": "Migrated from onefm_mcp's AI Chat Settings doctype (retired).",
   "fieldname": "observability_tab",
   "fieldtype": "Tab Break",
   "label": "Observability"
  },
  {
   "collapsible": 1,
   "description": "Site-wide LangSmith tracing defaults, consumed by onefm_mcp's Lumina observability layer as well as any one_bpmn agent that leaves its own LangSmith Project blank on AI Agent Configuration.",
   "fieldname": "section_break_observability",
   "fieldtype": "Section Break",
   "label": "LangSmith Observability"
  },
  {
   "default": "1",
   "fieldname": "enable_observability",
   "fieldtype": "Check",
   "label": "Enable Observability"
  },
  {
   "fieldname": "langsmith_api_key",
   "fieldtype": "Password",
   "label": "LangSmith API Key",
   "mandatory_depends_on": "eval:doc.enable_observability"
  },
  {
   "default": "lumina-production",
   "fieldname": "langsmith_project_name",
   "fieldtype": "Data",
   "label": "LangSmith Project Name",
   "mandatory_depends_on": "eval:doc.enable_observability"
  },
  {
   "default": "Partial",
   "fieldname": "observability_sanitization_level",
   "fieldtype": "Select",
   "label": "Observability Sanitization Level",
   "mandatory_depends_on": "eval:doc.enable_observability",
   "options": "None\nPartial\nFull"
  },
  {
   "default": "3",
   "fieldname": "observability_retry_attempts",
   "fieldtype": "Int",
   "label": "Observability Retry Attempts",
   "mandatory_depends_on": "eval:doc.enable_observability"
  },
  {
   "collapsible": 1,
   "description": "Credentials and schedule for the weekly email summary of the OneFM AI development agent's Langfuse traces.",
   "fieldname": "section_break_langfuse",
   "fieldtype": "Section Break",
   "label": "Langfuse & Weekly Agent Report"
  },
  {
   "default": "https://cloud.langfuse.com",
   "description": "Langfuse Cloud endpoint for the OneFM AI development agent observability",
   "fieldname": "langfuse_base_url",
   "fieldtype": "Data",
   "label": "Langfuse Base URL"
  },
  {
   "description": "Langfuse project public key (pk-lf-...)",
   "fieldname": "langfuse_public_key",
   "fieldtype": "Password",
   "label": "Langfuse Public Key"
  },
  {
   "fieldname": "column_break_langfuse",
   "fieldtype": "Column Break"
  },
  {
   "description": "Langfuse project secret key (sk-lf-...)",
   "fieldname": "langfuse_secret_key",
   "fieldtype": "Password",
   "label": "Langfuse Secret Key"
  },
  {
   "default": "0",
   "description": "Send a weekly email summary of agent performance metrics every Monday",
   "fieldname": "dev_agent_weekly_report_enabled",
   "fieldtype": "Check",
   "label": "Enable Weekly Report"
  },
  {
   "description": "Comma-separated email addresses to receive the weekly report",
   "fieldname": "dev_agent_report_recipients",
   "fieldtype": "Data",
   "label": "Report Recipients"
  },
  {
   "collapsible": 1,
   "description": "Consumed by one_bpmn's AI Skill token-estimate helper.",
   "fieldname": "section_break_token_estimator",
   "fieldtype": "Section Break",
   "label": "Token Estimation"
  },
  {
   "default": "4",
   "fieldname": "token_estimator_chars_per_token",
   "fieldtype": "Int",
   "label": "Chars Per Token (Estimate)"
  },
  {
   "description": "Migrated from onefm_mcp's AI Chat Settings doctype (retired). Config for onefm_mcp's Lumina chat widget and the MCP tool integrations it exposes — kept here rather than in onefm_mcp itself so agent-adjacent config has one home.",
   "fieldname": "mcp_integration_tab",
   "fieldtype": "Tab Break",
   "label": "MCP Integration"
  },
  {
   "default": "1",
   "description": "Enables onefm_mcp's Lumina chat widget.",
   "fieldname": "enabled",
   "fieldtype": "Check",
   "label": "Enable AI Chat Widget"
  },
  {
   "default": "0",
   "fieldname": "mcp_tools_enabled",
   "fieldtype": "Check",
   "label": "Enable MCP Tools"
  },
  {
   "default": "0",
   "fieldname": "enable_mcp_tools_for_user_planning_agent",
   "fieldtype": "Check",
   "label": "Enable MCP Tools For User Planning Agent"
  },
  {
   "description": "Has a real configured value in production — restore this field's consumer (believed to be the BA/User Planning agent's BPMN Script Task node) to read it from here.",
   "fieldname": "user_story_template",
   "fieldtype": "Small Text",
   "label": "User Story Template"
  },
  {
   "collapsible": 1,
   "fieldname": "jira_credentials_section",
   "fieldtype": "Section Break",
   "label": "Jira Credentials"
  },
  {
   "fieldname": "jira_url",
   "fieldtype": "Data",
   "label": "Jira URL"
  },
  {
   "fieldname": "jira_email",
   "fieldtype": "Data",
   "label": "Jira Email"
  },
  {
   "fieldname": "jira_api_token",
   "fieldtype": "Password",
   "label": "Jira API Token"
  },
  {
   "collapsible": 1,
   "fieldname": "copilot_settings_section",
   "fieldtype": "Section Break",
   "label": "Copilot Settings"
  },
  {
   "fieldname": "copilot_metrics_pat",
   "fieldtype": "Password",
   "label": "Copilot Metrics PAT",
   "mandatory_depends_on": "eval:doc.mcp_tools_enabled"
  },
  {
   "fieldname": "copilot_org_name",
   "fieldtype": "Data",
   "label": "Copilot Organization Name",
   "mandatory_depends_on": "eval:doc.mcp_tools_enabled"
  },
  {
   "fieldname": "copilot_enterprise_slug",
   "fieldtype": "Data",
   "label": "Copilot Enterprise Slug",
   "mandatory_depends_on": "eval:doc.mcp_tools_enabled"
  }
 ],
 "issingle": 1,
 "links": [],
 "modified": "2026-08-24 12:00:00.000000",
 "modified_by": "s.shariff@one-fm.com",
 "module": "ONE BPMN",
 "name": "Processa Settings",
 "owner": "Administrator",
 "permissions": [
  {
   "create": 1,
   "delete": 1,
   "email": 1,
   "read": 1,
   "role": "System Manager",
   "share": 1,
   "write": 1
  }
 ],
 "row_format": "Dynamic",
 "sort_field": "creation",
 "sort_order": "DESC",
 "states": [],
 "track_changes": 1
}
